import { Container, Typography } from "@material-ui/core";
import React from "react";

const Projects = () => {
  return (
    <Container>
      <Typography>This is projects page.</Typography>
    </Container>
  );
};

export default Projects;
